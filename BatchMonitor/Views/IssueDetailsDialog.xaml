<Window x:Class="BatchMonitor.Views.IssueDetailsDialog"        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"        Title="Issue Details"         Height="500"         Width="700"        WindowStartupLocation="CenterOwner"        Background="{DynamicResource MaterialDesignPaper}"        TextElement.Foreground="{DynamicResource MaterialDesignBody}"        TextElement.FontWeight="Regular"        TextElement.FontSize="13"        FontFamily="{DynamicResource MaterialDesignFont}"        ResizeMode="CanResize"        WindowStyle="SingleBorderWindow"        ShowInTaskbar="False">
    <Window.Resources>
        <!-- Error row style -->
        <Style x:Key="ErrorRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="#FFEBEE"/>
            <Setter Property="Foreground" Value="#C62828"/>
        </Style>
        <!-- Warning row style -->
        <Style x:Key="WarningRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="#FFF8E1"/>
            <Setter Property="Foreground" Value="#F57F17"/>
        </Style>
        <!-- Info row style -->
        <Style x:Key="InfoRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="#E3F2FD"/>
            <Setter Property="Foreground" Value="#1565C0"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- Header -->
        <!--<materialDesign:Card Grid.Row="0" Margin="16,16,16,8" Padding="16">
            <StackPanel>
                <TextBlock Name="HeaderTextBlock"                            Text="Batch Issues"                            Style="{DynamicResource MaterialDesignHeadline5TextBlock}"                           Margin="0,0,0,8"/>
                <TextBlock Name="SummaryTextBlock"                            Text="Summary will appear here"                           Style="{DynamicResource MaterialDesignBody1TextBlock}"                           Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            </StackPanel>
        </materialDesign:Card>-->
        <!-- Issues List -->
        <materialDesign:Card Grid.Row="1" Margin="16,8,16,8">
            <DataGrid Name="IssuesDataGrid"                      AutoGenerateColumns="False"                      CanUserAddRows="False"                      CanUserDeleteRows="False"                      CanUserReorderColumns="True"                      CanUserResizeColumns="True"                      CanUserResizeRows="False"                      CanUserSortColumns="True"                      GridLinesVisibility="Horizontal"                      HeadersVisibility="Column"                      SelectionMode="Single"                      SelectionUnit="FullRow"                      materialDesign:DataGridAssist.CellPadding="13,8,8,8"                      materialDesign:DataGridAssist.ColumnHeaderPadding="8">
                <DataGrid.Columns>
                    <!-- Type Column with Icon -->
                    <DataGridTemplateColumn Header="Type" Width="80" CanUserResize="False">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Width="16" Height="16" Margin="0,0,4,0">
                                        <materialDesign:PackIcon.Style>
                                            <Style TargetType="materialDesign:PackIcon">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Type}" Value="Error">
                                                        <Setter Property="Kind" Value="CloseCircle"/>
                                                        <Setter Property="Foreground" Value="#F44336"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}" Value="Warning">
                                                        <Setter Property="Kind" Value="AlertCircle"/>
                                                        <Setter Property="Foreground" Value="#FF9800"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}" Value="Info">
                                                        <Setter Property="Kind" Value="InformationOutline"/>
                                                        <Setter Property="Foreground" Value="#2196F3"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </materialDesign:PackIcon.Style>
                                    </materialDesign:PackIcon>
                                    <TextBlock Text="{Binding Type}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <!-- Line Number -->
                    <DataGridTextColumn Header="Line" Binding="{Binding LineNumber}" Width="60"/>
                    <!-- File Name -->
                    <DataGridTextColumn Header="File" Binding="{Binding FileName}" Width="120"/>
                    <!-- Message -->
                    <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
                    <!-- Timestamp -->
                    <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat='{}{0:HH:mm:ss}'}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
        <!-- Footer -->
        <Border Grid.Row="2"                 Background="{DynamicResource MaterialDesignToolBarBackground}"                Padding="16,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{DynamicResource MaterialDesignFlatButton}"                         Click="Refresh_Click"                         Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Refresh" Width="16" Height="16" Margin="0,0,4,0"/>
                        <TextBlock Text="Refresh"/>
                    </StackPanel>
                </Button>
                <Button Style="{DynamicResource MaterialDesignRaisedButton}"                         Click="Close_Click"                         IsDefault="True">Close</Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>