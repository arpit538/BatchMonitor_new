<Window x:Class="BatchMonitor.Views.ChangeScheduleDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Schedule Batch" Height="350" Width="450"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Schedule Daily Batch Execution" 
                   Style="{DynamicResource MaterialDesignHeadline5TextBlock}" 
                   Margin="0,0,0,24"/>

        <!-- Form Fields -->
        <StackPanel Grid.Row="1">
            <TextBlock Text="Select Daily Execution Time:" 
                       Style="{DynamicResource MaterialDesignSubtitle2TextBlock}" 
                       Margin="0,0,0,16"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <ComboBox Name="HourComboBox"
                          Grid.Column="0"
                          materialDesign:HintAssist.Hint="Hour (24-hour format)"
                          Style="{DynamicResource MaterialDesignFloatingHintComboBox}"
                          Margin="0,0,8,0"/>

                <ComboBox Name="MinuteComboBox"
                          Grid.Column="1"
                          materialDesign:HintAssist.Hint="Minute"
                          Style="{DynamicResource MaterialDesignFloatingHintComboBox}"
                          Margin="8,0,0,0"/>
            </Grid>

            <TextBlock Text="This will run daily at the specified time" 
                       Style="{DynamicResource MaterialDesignCaptionTextBlock}" 
                       Opacity="0.7"
                       Margin="0,16,0,0"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,24,0,0">

            <Button Style="{DynamicResource MaterialDesignFlatButton}" 
                    Click="Cancel_Click" 
                    Margin="0,0,8,0">
                Cancel
            </Button>

            <Button Style="{DynamicResource MaterialDesignRaisedButton}" 
                    Click="OK_Click" 
                    IsDefault="True">
                Schedule
            </Button>
        </StackPanel>
    </Grid>
</Window>